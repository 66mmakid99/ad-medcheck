# 병원 필터링 로직 수정

## ❌ 현재 문제
- 비뇨기과, 이비인후과, 내과 등 전부 크롤링 중
- 피부과/성형외과만 필터링하면 GP(일반의원) 놓침
- 실제 피부 시술하는 GP가 더 많음!

---

## ✅ 해결: 시술명 키워드 기반 필터링

### 필터링 조건 (OR 조건)

```javascript
// 1. 진료과목 기반
const TARGET_CATEGORIES = ['피부과', '성형외과'];

// 2. 병원명 키워드
const NAME_KEYWORDS = ['피부', '스킨', '더마', '성형', '뷰티', '클리닉'];

// 3. 시술명 키워드 (핵심!)
const PROCEDURE_KEYWORDS = [
  // HIFU
  '울쎄라', '슈링크', '리프테라', '더블로', '울트라포머', '하이푸',
  
  // RF
  '써마지', '올리지오', '인모드', '토르RF', '아그네스', '스카렛', '포텐자',
  
  // 스킨부스터
  '리쥬란', '쥬베룩', '물광', '연어주사', '엑소좀', '프로파일로',
  
  // 보톡스
  '보톡스', '보툴렉스', '나보타', '제오민', '디스포트',
  
  // 필러
  '필러', '쥬비덤', '레스틸렌', '벨로테로', '클레비엘',
  
  // 레이저
  '피코레이저', '피코슈어', '레이저토닝', '프락셀', '클라리티', 'IPL',
  
  // 리프팅
  '실리프팅', '민트실', 'PDO', '코그실', '리프팅'
];
```

### 필터링 로직

```javascript
function shouldIncludeHospital(hospital) {
  const name = hospital.name || '';
  const category = hospital.category || '';
  const services = hospital.services || '';
  
  // 1. 진료과목 체크
  if (TARGET_CATEGORIES.some(cat => category.includes(cat))) {
    return true;
  }
  
  // 2. 병원명 키워드 체크
  if (NAME_KEYWORDS.some(kw => name.includes(kw))) {
    return true;
  }
  
  // 3. 시술명 키워드 체크 (홈페이지/네이버플레이스에서 발견된 경우)
  const allText = `${name} ${services}`.toLowerCase();
  if (PROCEDURE_KEYWORDS.some(kw => allText.includes(kw.toLowerCase()))) {
    return true;
  }
  
  return false;
}
```

---

## 📋 전체 키워드 목록 (200개+)

### HIFU (13개)
울쎄라, 슈링크, 슈링크유니버스, 리프테라, 더블로, 더블로골드, 울트라포머, 울트라포머MPT, 유쎄라, 하이푸, 울핏, 포텐자HIFU

### RF (22개)
써마지, 써마지FLX, 써마지CPT, 올리지오, 인모드, 인모드FX, 인모드미니FX, 텐써마, 엑실리스, 토르RF, 아그네스, 스카렛, 시크릿RF, 인피니, 포텐자, 비바체, 볼뉴머

### 스킨부스터/주사 (28개)
리쥬란, 리쥬란힐러, 리쥬란아이, 리쥬란HB, 리쥬란톤, 쥬베룩, 쥬베룩볼륨, 쥬베룩울트라, 물광주사, 연어주사, 아기주사, 스킨보톡스, 윤광주사, 핑크주사, 백옥주사, 신데렐라주사, 비타민주사, 엑소좀, 줄기세포주사, 프로파일로, 스컬트라, 엘란쎄, 레디에스

### 보톡스 (20개)
보톡스, 제오민, 나보타, 보툴렉스, 디스포트, 리즈톡스, 코어톡스, 이노톡스, 메디톡신, 스킨보톡스, 미소보톡스, 턱보톡스, 광대보톡스, 승모근보톡스, 종아리보톡스, 이마보톡스, 미간보톡스

### 필러 (28개)
쥬비덤, 쥬비덤볼륨, 쥬비덤볼루마, 쥬비덤볼벨라, 레스틸렌, 레스틸렌리도, 벨로테로, 테오시알, 이브아르, 클레비엘, 스타일레이지, 프린세스, 코필러, 팔자필러, 볼필러, 턱필러, 이마필러, 입술필러, 눈밑필러

### 레이저/토닝 (30개)
피코레이저, 피코슈어, 피코웨이, 피코플러스, 레이저토닝, 클라리티, 제네시스, 프락셀, 스펙트라, 레블라이트, IPL, BBL, LDM, 아쿠아필, 제트필, 탄소레이저, 블랙돌, 레이저제모, 젠틀맥스, 소프라노

### 리프팅/실 (20개)
실리프팅, 미니실리프팅, 민트실, 녹는실, 코그실, 브이리프팅, 아큐리프트, 애플라인, 미스코실, PDO실, PCL실, PLLA실

---

## 🔄 수정된 크롤링 플로우

```
1단계: 공공데이터 API로 전체 의원 수집
         ↓
2단계: 필터링 (진료과목 OR 병원명 OR 시술키워드)
         ↓  
3단계: 네이버 플레이스/구글 검색으로 홈페이지 URL 보강
         ↓
4단계: 홈페이지에서 시술 키워드 추가 확인 → 필터링 보강
         ↓
5단계: 최종 CSV 저장
```

---

## 📊 예상 결과

| 단계 | 수량 |
|------|------|
| 전체 의원 (서울) | ~15,000개 |
| 1차 필터링 (피부과+성형외과+키워드) | ~3,000개 |
| 홈페이지 URL 보유 | ~1,500개 |

---

## 작업

1. crawler.js에 필터링 함수 추가
2. 키워드 배열 config로 분리
3. 서울 테스트 다시 실행
