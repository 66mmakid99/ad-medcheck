{
  "name": "medcheck-engine",
  "version": "1.1.0",
  "type": "module",
  "description": "Medical Advertisement Compliance Engine",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=neutral --outfile=dist/index.js --external:__STATIC_CONTENT_MANIFEST --tree-shaking=true --minify",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "medical",
    "advertisement",
    "compliance",
    "korea"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "hono": "^4.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260129.0",
    "@types/node": "^25.1.0",
    "esbuild": "^0.27.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.69.0"
  }
}
